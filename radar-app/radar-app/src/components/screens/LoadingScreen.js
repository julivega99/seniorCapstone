import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const LoadingScreen = ({navigation}) => {
    return (
        <View><Text>LoadingScreen</Text></View>
    );
}

export default LoadingScreen;